{
  "scripts": {
    "build": "gitbook build",
    "serve": "gitbook serve",
    "deploy": "npm run build && gh-pages -d _book",
    "dep": "gitbook install"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "gitbook-cli": "^2.3.2"
  },
  "dependencies": {
    "gitbook-plugin-favicon": "0.0.2",
    "gitbook-plugin-url-embed": "^1.0.0"
  }
}
